# E2E Test Fixtures

This directory contains fixture files for E2E tests.

## Files

- `sample.mpp` - Sample Microsoft Project file for testing MPP conversion
- `downloaded-result.xml` - (Generated) Downloaded conversion result (cleaned after test)

## Notes

The `sample.mpp` file is copied from `scripts/samples/sample.mpp` in the project root.
If you need a different test file, replace it here.

For real MPP conversion testing, ensure the MPP converter microservice is running.
