'use client';

import ConverterTemplate, { converterConfigs } from '@/components/converters/ConverterTemplate';

export default function ImageWhatsappPage() {
  return <ConverterTemplate config={converterConfigs['image-whatsapp']} />;
}
