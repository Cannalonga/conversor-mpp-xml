'use client';

import ConverterTemplate, { converterConfigs } from '@/components/converters/ConverterTemplate';

export default function VideoReelsPage() {
  return <ConverterTemplate config={converterConfigs['video-reels']} />;
}
